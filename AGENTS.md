# MeshProbe

## Releases

- Before every release, compare the changelog with the complete Git history since the previous
  release tag. Review both `git log <previous-tag>..HEAD` and the public-facing diff so every
  user-visible feature, behavior change, and fix is represented. Omit CI-only or internal changes
  only deliberately, and record those exclusions in the release PR.
